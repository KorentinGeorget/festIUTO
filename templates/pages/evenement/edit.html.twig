{% extends 'base.html.twig' %}

{% block title %}Edit Evenement{% endblock %}

{% block body %}
    <h1>Edit Evenement</h1>

    {{ include('pages/evenement/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('evenement.index') }}">back to list</a>

    {{ include('pages/evenement/_delete_form.html.twig') }}
{% endblock %}
